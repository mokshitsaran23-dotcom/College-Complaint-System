# Git Branch & Commit Commands

```bash
git checkout -b feature/SCRUM-23-student-complaint-flow
git add .
git commit -m "SCRUM-23: Student submits -> admin assigns -> tracked to resolution (SCRUM02-E2E-001)"
```
